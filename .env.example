# In dev this is optional — it defaults to 127.0.0.1:1999 (npx partykit dev).
# After deploying with `npx partykit deploy`, set this to your project's host,
# e.g. party-hub.yourname.partykit.dev
VITE_PARTYKIT_HOST=127.0.0.1:1999
